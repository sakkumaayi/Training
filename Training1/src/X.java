/**
 * 
 */

/**
 * @author santh
 *
 */
public class X {

	/**
	 * To import the classes from one project to another project 
	 * first we have to export the java project in to Jar file(java archive) file 
	 * to bundles files to one file we use archives or zip files
	 * we have unzip the Jar file to see the files inside the jar file
	 * then we have to add this jar file to project build path -> configure build path-> add external archives in libraries tab
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		com.training.A obj = new com.training.A();

	}

}
